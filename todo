=> command line argumment parsing:
	-> -u --usage			(print usage)
	-> -h --help			(print help)
	-> -r --rom [path]		(defaut argument)
	-> --gui				(use GUI window, default)
	-> --cli				(display in the terminal)
	-> -s --scale [amount]	(set window scale, GUI only)
	-> -g --grid 			(show pixel gird, GUI only)
	-> -d --debug 			(show debug information)

=> Rendering 
	-> SDL2
 OK -> CLI

=> debug (in CLI)
	-> stop, steb-by-step
 OK	-> show all register (Vx DT ST I SP PC) + stack content
 OK	-> show dilsplay in terminal

=> inputs
	-> add CLI keyboard suport
	-> add GUI keyboard suport
	-> add opcode keyboard suport

=> Disasembler
